# Installs the vyas package itself (pyproject.toml, editable) plus its
# runtime deps (fastapi/httpx/rapidfuzz/pydantic/python-multipart).
-e .

# Only needed for the standalone demo (main.py) and local dev — not part of
# the vyas package's own dependencies, since a parent app supplies its own
# server process and env loading.
uvicorn[standard]>=0.32
python-dotenv>=1.0
pytest>=8.3
